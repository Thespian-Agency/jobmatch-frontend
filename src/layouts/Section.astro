---
interface Props {
  className?: string;
  pt?: boolean;
  pb?: boolean;
  id?: string;
}

const { className, pt = false, pb = false, id } = Astro.props;
---

<section
  class:list={[
    "flex relative tablet:px-80 max-w-screen-xldesktop px-16 w-full justify-center",
    {
      "pt-128": pt,
      "pb-128": pb,
    },
  ]}
  id={id}
>
  <div class:list={["flex container w-full", className]}>
    <slot />
  </div>
</section>
